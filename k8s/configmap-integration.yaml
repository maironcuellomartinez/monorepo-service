apiVersion: v1
kind: ConfigMap
metadata:
  name: integration-config
  namespace: event-corner
data:
  NODE_ENV: "production"
  PORT: "3008"
  HOST: "0.0.0.0"
  SNOWQ_BASE_URL: "http://api-snowq-service:3090"
  ABAC_URL: "http://abac-microservice:3005"
  SERVICENOW_USE_SNOWQ: "true"
  # ABAC_M2M_TOKEN → Secret integration-secrets
  # DROPPOINT_USERNAME / DROPPOINT_PASSWORD → Secret integration-secrets
